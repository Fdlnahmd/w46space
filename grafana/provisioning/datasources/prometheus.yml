apiVersion: 1

datasources:
  - name: Office Rent Prometheus
    uid: office-rent-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    editable: true
    jsonData:
      timeInterval: 15s
